# Imported System Requirements Slice: Command Activation Surface

## Document Control

- Slice ID: `command-activation-surface-v1`
- Source baseline: `v1.3.16`
- Target feature: `command-activation-surface-v1`
- Import status: public-safe requirements core

## System Context

This import carries one software requirement, `VHS-REQ-594`. It supports the
public MIT authority's VS Code extension surface by requiring command-driven
activation for the real product surface, documentation route, and runtime
settings CLI preparation.

No additional system requirement IDs are imported by this packet. Future
public-local system requirements may be added for divergent MIT behavior, but
they must not redefine imported requirement IDs.

## Import Notes

This slice is import/spec-only. It does not admit extension manifest changes,
command handlers, documentation panels, runtime settings CLI materialization,
compare execution, packaging, or Marketplace publication.
