# Imported Software Requirements Slice: Runtime Contract Host Provider

## Document Control

- Slice ID: `runtime-contract-host-provider-v1`
- Source baseline: `v1.3.16`
- Target feature: `runtime-contract-host-provider-v1`
- Import status: public-safe requirements core

## Software Requirements

| ID | Requirement | Rationale | Fit Criterion | Verification |
| --- | --- | --- | --- | --- |
| VHS-REQ-094 | Governed runtime-proof override inputs shall allow explicit LabVIEW CLI path, LabVIEW executable path, and bitness override inputs ahead of auto-discovery, while keeping path-level overrides off installed-user manifest settings. | Explicit proof paths are useful for controlled validation, but installed-user settings should stay simpler. | Runtime selection honors explicit proof overrides, fails closed when an explicit path is missing, and does not expose path-level overrides as public settings. | Unit tests |
| VHS-REQ-095 | Windows runtime discovery shall probe registry views and documented installation roots to locate LabVIEW 2025-or-newer executables and comparison tooling. | LabVIEW 2025 and newer can create comparison reports for older VIs without migration. | Discovery scans documented Windows roots, rejects LabVIEW 2024-or-older requests with a stable unsupported-version reason, and classifies host-native candidates. | Unit tests |
| VHS-REQ-138 | The report-generation architecture shall preserve a runtime-provider boundary for Windows 64-bit execution so an isolated LabVIEW 2026 Q1 Windows container path remains available as a bounded expert lane without colliding with host-native 64-bit LabVIEW. | Provider choice must stay explicit while retaining an isolation path for advanced validation. | Architecture records state that Windows 64-bit report execution remains behind a provider boundary. | Documentation review |
| VHS-REQ-141 | The stored comparison-report HTML shall render runtime provider, engine, blocked reason, selected tool paths, and retained runtime notes. | Report artifacts should expose runtime readiness facts without requiring metadata inspection. | Rendered report HTML includes runtime-selection facts. | Unit test |
| VHS-REQ-144 | A ready LabVIEW CLI runtime selection shall map to a `CreateComparisonReport` command plan with asset-backed HTML, staged VI paths, output path, and explicit LabVIEW selection fields. | The LabVIEW CLI is the primary report engine and must retain exact runtime facts. | The execution-plan builder returns a governed LabVIEWCLI command line with staged VI paths and selection fields. | Unit test |
| VHS-REQ-146 | Runtime architecture shall retain a LabVIEW 2026 Q1 Windows container provider as a bounded Windows 64-bit expert isolation path while keeping host-default installed compare explicit. | The system needs retained Windows-container proof without claiming Docker-first installed-user behavior. | Architecture and proof surfaces preserve the host-default plus bounded expert-provider split. | Documentation review and smoke proof |
| VHS-REQ-148 | The host-native report-execution runner shall execute the governed command plan exactly, retain stdout/stderr artifacts, and record exit code plus duration for the attempted NI tool invocation. | Developers need explicit execution evidence and troubleshooting facts. | Runtime execution records stdout, stderr, command, exit code, duration, and packet metadata. | Unit test |
| VHS-REQ-194 | The canonical public report engine shall remain LabVIEWCLI `CreateComparisonReport`, with no public engine selector or LVCompare path override surface. | Public behavior should not expose unsupported engine switching. | CLI and runtime selection keep LabVIEWCLI as the public report engine. | Unit tests |
| VHS-REQ-588 | The authority line shall retain Linux host LabVIEW 2026 Community proof as a distinct admitted evidence class from Windows installed-user proof. | Linux host success must not masquerade as Windows proof. | Proof and docs distinguish Linux host proof from Windows proof. | Unit tests, live proof review, and documentation review |
| VHS-REQ-589 | The public validation lane shall expose `vihs validate-fixture` as an installed CLI command that runs the canonical public fixture and writes proof JSON plus issue body. | Users need a repeatable way to file structured validation evidence. | The fixture command writes proof artifacts, retains selected runtime facts, and documents proof-status boundaries. | Unit tests, live fixture proof, and documentation review |
| VHS-REQ-590 | The Windows Docker Desktop Windows-container proof intake shall require a real Windows host, Docker Desktop OSType `windows`, canonical fixture command, and retained generated-report evidence. | This proof must not be closed by Linux Docker, WSL, host-provider proof, or reports without proof packets. | The intake template and runtime proof rules retain Docker OSType, runtime provider, runtime engine, execution state, and generated-report facts. | Unit tests and documentation review |
