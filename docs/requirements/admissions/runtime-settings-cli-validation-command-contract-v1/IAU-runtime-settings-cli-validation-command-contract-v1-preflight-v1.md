# IAU Preflight: Runtime Settings CLI Validation Command Contract

Status: `pass`

Implementation start is allowed after this admission PR merges and a separate
implementation handoff issue is created:
`IAU-runtime-settings-cli-validation-command-contract-v1`

Admitted start scope: T009-T018.

Still blocked: T019-T027, OS inspection, runtime locator invocation, private
path discovery, runtime validation execution, compare execution, LabVIEWCLI
execution, Docker execution or orchestration, raw terminal process wiring, live
terminal proof, package/bin publication, launcher/profile mutation, VSIX
packaging, release automation, Marketplace, `validate-plan-only`, and source
copying behavior.

Implementation sharing: `none`. Marketplace publication: `disabled`.
